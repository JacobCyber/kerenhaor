---
import Layout from "@/layouts/Layout.astro";
import PageContactContent from "@/components/PageContactContent.astro";
import { t } from "@/i18n";

const locale = "en";
const pageTitle = t(locale, "contactPage.title");
---

<Layout title={pageTitle} locale={locale}>
	<PageContactContent locale={locale} />
</Layout>
